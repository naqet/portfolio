<main id="main">
  <h1>Your message has been <span class="gradient-text">delivered</span></h1>
  <a href="/" tabindex="0" aria-label="Go back to home page"
    >Go back to home page
  </a>
</main>

<style lang="scss">
  main {
    display: grid;
    place-items: center;
    position: relative;
  }

  a {
    border: 2px solid var(--secondary-text);
    padding: 0.5rem 1rem;
    color: var(--primary-text);

    &:focus,
    &:hover {
      outline: none;
      border: 2px solid var(--blue);
    }
  }

  h1 {
    text-align: center;

    &::before {
      content: "*";
      white-space: nowrap;
      position: absolute;
      font-size: 15rem;
      color: var(--secondary-text);
      opacity: 0.08;
      top: 50%;
      left: 50%;
      z-index: -1;
      transform: translate(-50%, -50%);
    }
  }
</style>
