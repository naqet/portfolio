---
const originUrl = new URL(Astro.request.url).origin;
---

<main id="main">
  <div class="contact-info">
    <h1>Contact me</h1>
    <p>
      Perhaps send me a message if you've come this far? I'm always open to new
      opportunities, whether they involve working for your company full-time,
      doing side work as a freelancer, or contributing to open source.
    </p>
    <p>
      I am aware that some individuals dislike forms... I'm always available on <a
        href="https://www.linkedin.com/in/naqet/"
        target="_blank"
        rel="noreferrer"
        class="gradient-underline"
        aria-label="Go to my LinkedIn profile">LinkedIn</a
      > if you want to connect.
    </p>
    <p>Take care and have a nice day!</p>
  </div>
  <form
    action="https://formsubmit.co/s.t.szad+portfolio@gmail.com"
    method="POST"
  >
    <div class="input-box">
      <input
        id="name"
        name="name"
        type="text"
        required
        placeholder=" "
        aria-label="Your or company name"
      />
      <label for="name">Name</label>
    </div>

    <div class="input-box">
      <input
        id="email"
        name="email"
        type="email"
        required
        placeholder=" "
        aria-label="Email to receive answer to"
      />
      <label for="email">Email</label>
    </div>

    <div class="input-box">
      <input
        id="_subject"
        name="_subject"
        type="text"
        required
        placeholder=" "
        aria-label="Topic of the message"
      />
      <label for="_subject">Topic</label>
    </div>

    <div class="input-box">
      <textarea
        id="message"
        name="message"
        rows="8"
        required
        placeholder=" "
        aria-label="Content of the message"></textarea>
      <label for="message">Message</label>
    </div>
    <input type="hidden" name="_next" value={originUrl + "/thank-you"} />
    <input type="text" name="_honey" style="display:none" />
    <button type="submit">Send</button>
  </form>
</main>

<style lang="scss">
  main {
    padding: 2rem;
    display: grid;
    gap: 3rem;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .input-box {
    position: relative;
  }

  input,
  textarea {
    background-color: transparent;
    border: 1px solid var(--secondary-text);
    color: var(--primary-text);
    font-size: 1.2rem;
    padding: 0.5rem;
    border-radius: 8px;
    width: -webkit-fill-available;
    width: -moz-available;
    caret-color: var(--blue);

    &:focus,
    &:hover {
      border-color: var(--blue);
      outline: none;

      & ~ label {
        top: -0.4rem;
        font-size: 0.8rem;
        padding-inline: 0.5rem;
        background-color: var(--bg);
      }
    }

    &:valid {
      border-color: var(--light-blue);
      caret-color: var(--light-blue);
    }

    &:not(:placeholder-shown) {
      &:invalid {
        border-color: var(--error);
        caret-color: var(--error);
      }

      &:not(:focus) ~ label {
        top: -0.5rem;
        font-size: 0.8rem;
        padding-inline: 0.5rem;
        background-color: var(--bg);
      }
    }
  }

  /* This overrides default autocomplete styles */
  input:-webkit-autofill,
  input:-webkit-autofill:hover,
  input:-webkit-autofill:focus,
  input:-webkit-autofill:active {
    transition: background-color 5000s ease-in-out 0s;
    -webkit-text-fill-color: var(--light-blue) !important;
  }

  textarea {
    resize: vertical;
    min-height: 204px;
  }

  label {
    position: absolute;
    top: 0.7rem;
    left: 0.7rem;
    border-radius: 8px;
    transition:
      0.3s font-size ease,
      0.3s top ease,
      0.3s padding-inline ease,
      0.3s background-color ease;
  }

  button {
    border: 2px solid var(--secondary-text);
    border-radius: 8px;
    padding: 0.5rem 1rem;
    font-size: 1rem;
    color: var(--primary-text);
    margin: auto;
    background: transparent;
    cursor: pointer;
    outline: none;

    &:hover,
    &:focus {
      border-color: var(--blue);
      outline: none;
    }
  }

  a {
    color: var(--primary-text);
  }

  .contact-info > p:not(:last-child) {
    line-height: 2;
    margin-block: 2rem;
  }

  @media screen and (min-width: 768px) {
    main {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
